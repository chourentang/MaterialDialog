<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp">

    <TextView
        android:id="@+id/title"
        style="@style/MaterialDialog.TextView.Title"
        tools:text="Title" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="20dp"
            tools:text="Lorem ipsum doler samet Jane is awesome guy and Kaylee is so hot." />

        <FrameLayout
            android:id="@+id/custom_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <ListView
            android:id="@+id/list"
            style="@style/MaterialDialog.ListView" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/button_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            style="@style/MaterialDialog.Button.Other"
            android:visibility="invisible"
            tools:text="other button"/>

        <Button
            android:id="@+id/positive"
            style="@style/MaterialDialog.Button"
            tools:text="Positive"/>

        <Button
            android:id="@+id/negative"
            style="@style/MaterialDialog.Button"
            tools:text="Negative"/>
    </LinearLayout>

</LinearLayout>